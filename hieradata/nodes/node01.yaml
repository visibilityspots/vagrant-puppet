---
classes:
  - roles::puppetclient
  - roles::elasticsearch

puppet::runmode: none

elasticsearch::manage_repo: true
elasticsearch::repo_version: '1.4'
elasticsearch::java_install: true

elasticsearch::config:
  cluster.name: cluster

elasticsearch::instance:
  es-01:
    config:
      node.name:
        node01
