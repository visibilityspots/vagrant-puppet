---
:hierarchy:
        - "nodes/%{::hostname}"
        - "roles/%{::role}"
        - common

:backends:
        - yaml

:yaml:
        :datadir: '/etc/puppetlabs/code/environments/production/hieradata/'

:merge_behaviour:
        :deeper
